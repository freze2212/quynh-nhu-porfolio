"use client"

import { useState } from "react"
import Image from "next/image"
import Link from "next/link"

import { Camera, ClosedCaption, Hamburger, Home, Menu, X } from "lucide-react"

import { menuData } from "@/data/menu-data"

import { Button } from "../ui/button"
import { Logo } from "../widgets"

export default function Header() {
	const [isMenuOpen, setIsMenuOpen] = useState(false)

	const toggleMenu = () => {
		setIsMenuOpen(!isMenuOpen)
	}

	return (
		<header className="fixed top-0 left-0 w-full bg-white/30 backdrop-blur-md shadow-md z-20 h-[80px]">
			<div className="flex flex-row items-center justify-between justify-items-center gap-10 mt-3 max-w-7xl mx-auto">
				<Link href="/" className="text-xl font-bold text-gray-800">
					<div className="w-[80px]">
						<Image
							src="/images/logo.png"
							alt="Quỳnh Như"
							title="Quỳnh Như"
							width={512}
							height={512}
							priority
						/>
					</div>
				</Link>

				<nav className="hidden space-x-6 md:flex">
					{menuData &&
						menuData.map((item) => (
							<Link
								key={item.id}
								href="#"
								className="text-white hover:text-[#d6ae5b]"
							>
								{item.title}
							</Link>
						))}
				</nav>

				<div className="hidden md:block">
					{/* <Button
					className="bg-[#d6ae5b]"
					type="button"
					size={"lg"}
					variant={"outline"}
				>
					Liên hệ
				</Button> */}

					{/* <Link
					href={"/"}
					className="w-full px-6 py-2.5 text-sm font-medium tracking-wider text-white transition-colors duration-300 transform md:w-auto md:mx-4 focus:outline-none bg-[#d6ae5b] rounded-lg hover:bg-[#d6ae5b] focus:ring focus:ring-[#d6ae5b] focus:ring-opacity-80"
				>
					Liên hệ
				</Link> */}
				</div>

				<button
					className="md:hidden"
					onClick={toggleMenu}
					aria-label="Toggle menu"
				>
					{isMenuOpen ? <X size={24} /> : <Menu size={24} />}
				</button>
			</div>

			{isMenuOpen && (
				<div className="">
					<div className="absolute inset-0 left-0 bottom-0 bg-black bg-opacity-50 min-h-screen min-w-screen"></div>
					<nav className="absolute inset-0 left-0 top-16 min-w-full min-h-full flex-col p-4 md:hidden flex items-center justify-between z-20">
						{menuData &&
							menuData.map((item) => (
								<Link
									key={item.id}
									href="#"
									className="py-2 text-white hover:text-[#d6ae5b]"
								>
									{item.title}
								</Link>
							))}

						{/* <div className="flex flex-col md:flex-row gap-4">
							<Link
								href="#"
								className="px-8 py-4 bg-gradient-to-r from-orange-500 via-orange-500 to-yellow-500 text-white font-semibold rounded-full shadow-lg transform hover:scale-105 transition duration-300 ease-in-out text-lg"
							>
								Liên hệ ngay
							</Link>
							<Link
								href="#"
								className="px-8 py-4 border-2 border-white text-white font-semibold rounded-full hover:bg-white hover:text-gray-900 transition duration-300 ease-in-out text-lg"
							>
								Đặt lịch tư vấn
							</Link>
						</div> */}
					</nav>
				</div>
			)}
		</header>
	)
}
